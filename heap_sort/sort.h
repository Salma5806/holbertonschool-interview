#ifndef SORT_H
#define SORT_H

#include <stddef.h>
#include <stdio.h>

/* Prototype donné */
void heap_sort(int *array, size_t size);

/* Fonction utilitaire (déjà fournie dans ton projet) */
void print_array(const int *array, size_t size);

#endif /* SORT_H */